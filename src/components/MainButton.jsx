
const MainButton = ({text, className}) => {
  return (
    <button className={`py-[16px] px-[48px] font-Poppins font-medium text-[16px] leading-[24px] cursor-pointer ${className}`}><a href="#"></a>{text}</button>
  )
}
export default MainButton